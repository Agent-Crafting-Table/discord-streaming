{
  "name": "discord-streaming",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "bun install --no-summary && sh -c 'while true; do bun src/server.ts; sleep 0.5; done'"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "discord.js": "^14.0.0",
    "zod": "^3.0.0"
  }
}
